{
  "name": "@bip/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --watch",
    "lint": "biome lint",
    "prisma:generate": "cd src/_shared/prisma && doppler run -- prisma generate",
    "prisma:generate:prod": "cd src/_shared/prisma && prisma generate",
    "prisma:migrate:dev": "cd src/_shared/prisma && doppler run -- prisma migrate dev",
    "prisma:migrate:create": "cd src/_shared/prisma && doppler run -- prisma migrate dev --create-only",
    "prisma:migrate:baseline": "cd src/_shared/prisma && doppler run -- prisma migrate resolve --applied 20250301154946_init",
    "prisma:introspect": "cd src/_shared/prisma && doppler run -- prisma db pull",
    "prisma:studio": "cd src/_shared/prisma && doppler run -- prisma studio",
    "prisma:reset": "cd src/_shared/prisma && doppler run -- prisma migrate reset --force",
    "migrate:users": "doppler run -- bun run scripts/migrate-users-to-supabase.ts"
  },
  "dependencies": {
    "@bip/domain": "workspace:*",
    "@prisma/adapter-pg": "^7.2.0",
    "@prisma/client": "^7.2.0",
    "@supabase/supabase-js": "^2.89.0",
    "dotenv": "^17.2.3",
    "postgres": "^3.4.7",
    "redis": "^5.10.0",
    "winston": "^3.19.0",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/pg": "^8.16.0",
    "pg": "^8.16.3",
    "prisma": "^7.2.0",
    "supabase": "^2.70.4",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
