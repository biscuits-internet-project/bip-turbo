{
  "key": "ratings.toggle-visible",
  "description": "Whether a user sees the opt-in control for the calibrated show rating (the toggle in profile settings). Open to everyone. Users without the toggle keep an unset preference and fall to ratings.default-calibrated. Launch state: everyone.",
  "type": "feature_flag",
  "valueType": "bool",
  "default": {
    "rules": [
      {
        "criteria": [
          {
            "operator": "ALWAYS_TRUE"
          }
        ],
        "value": {
          "type": "bool",
          "value": true
        }
      }
    ]
  }
}
